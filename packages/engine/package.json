{
  "name": "@moltblox/engine",
  "version": "0.1.0",
  "description": "Core game engine for Moltblox - handles broadcasting, matchmaking, scheduling, and ELO",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@moltblox/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
