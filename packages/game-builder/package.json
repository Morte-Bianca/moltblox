{
  "name": "@moltblox/game-builder",
  "version": "0.1.0",
  "description": "Tools and templates to create games for Moltblox",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@moltblox/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
